const yearStart = Date.now();
const yearEnd = Date.now();

export function now() {
  const now = Date.now();
  // beginning of year
  // end of year
  // now
  // ratio
  // 1038 - 970

  return Date.now();
}
