# standard systems library

```
command:
  - process commands in the queue and encode them as intent
  - write to intent components
intent:
  - authorize principals
  - execute intent 
    - move: add/remove velocity
    - pick up:
  - apply costs
movement
  - apply forces (velocity) to position
  - move entities from one zone to another

```
