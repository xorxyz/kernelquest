#!/bin/sh
# run the game client

set -e
save_state=$(stty -g)

while true; 
do
  stty raw -echo
  nc localhost 3000 || printf 'oops!'
  stty "$save_state"
  clear
  printf 'connecting...'
  sleep 2
done

